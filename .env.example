# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ============================================
# DATABASE CONFIGURATION (Choose One)
# ============================================

# Option 1: Supabase PostgreSQL (Recommended)
# Create free account at https://app.supabase.com
# Get connection string from: Project → Settings → Database → Connection string (URI)
DATABASE_URL="postgresql://postgres:[password]@[host]:[port]/postgres?sslmode=require"

# Option 2: Local PostgreSQL (for development)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/data_analytics"

# Option 3: PlanetScale MySQL (Alternative)
# DATABASE_URL="mysql://[username]:[password]@[host]/[database]?sslaccept=strict"

# ============================================
# SENTRY ERROR MONITORING (Optional but Recommended)
# ============================================

# Create account at https://sentry.io and get your DSN

# Client-side Sentry DSN (used in browser)
NEXT_PUBLIC_SENTRY_DSN="https://your-key@your-sentry-domain.ingest.sentry.io/your-project-id"

# Server-side Sentry DSN (used in Node.js)
SENTRY_DSN="https://your-key@your-sentry-domain.ingest.sentry.io/your-project-id"

# Optional: For uploading source maps to Sentry (production only)
# SENTRY_ORG="your-org-name"
# SENTRY_PROJECT="your-project-name"
# SENTRY_AUTH_TOKEN="your-auth-token"

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# Environment: development, production, test
NODE_ENV=development
# Set this in your .env for local development
# mysql://user:password@127.0.0.1:3306/database_name
DATABASE_URL="mysql://<DB_USER>:<DB_PASSWORD>@localhost:3306/<database_name>"

# Add other environment variables here